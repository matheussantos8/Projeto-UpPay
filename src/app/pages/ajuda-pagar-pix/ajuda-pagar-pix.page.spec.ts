import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AjudaPagarPixPage } from './ajuda-pagar-pix.page';

describe('AjudaPagarPixPage', () => {
  let component: AjudaPagarPixPage;
  let fixture: ComponentFixture<AjudaPagarPixPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AjudaPagarPixPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
