import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AjudaReceberPixPage } from './ajuda-receber-pix.page';

describe('AjudaReceberPixPage', () => {
  let component: AjudaReceberPixPage;
  let fixture: ComponentFixture<AjudaReceberPixPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AjudaReceberPixPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
